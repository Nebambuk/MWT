{
  "name": "mwt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "build:firefox": "tsc && vite build",
    "dev:firefox": "web-ext run -s dist --target firefox-desktop",
    "lint": "web-ext lint -s dist"
  },
  "devDependencies": {
    "@types/jquery": "^4.0.1",
    "@types/node": "^26.1.0",
    "chrome-types": "^0.1.432",
    "sass": "^1.101.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "web-ext": "^10.4.0"
  },
  "dependencies": {
    "jquery": "^4.0.0"
  }
}
